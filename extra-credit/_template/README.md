# <repo name>

> One line: what this is and why it exists. Cut everything else.

Tier: experiment | tool · Status: active | parked | graduated

## What it does

Two or three sentences. The problem, the approach, the result. No filler.

## Run it

```
# the exact command(s). Offline / stdlib-first where it fits.
python main.py
```

## Connections

How this ties back to the logged material — the edges that make it a node in the graph, not an
island. Populated by the connect protocol (see [`../CONTEXT.md`](../CONTEXT.md)). Each entry is a
real link to a lesson, dossier, build-log ledger, GBrain page, or memory:

- **<area>** — a real markdown link to the lesson / dossier / build-log ledger / GBrain page /
  memory it connects to, then one line on *why* it connects.
- Add one per genuine edge. If a scan finds none, say so — that absence is information too.

## Notes

Anything a future reader (or agent) needs to pick this up cold.
