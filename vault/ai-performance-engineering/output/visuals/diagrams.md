# Visuals — Diagrams

> No visuals collected. The inference-serving keep chapters (ch15–20) and the `docs/` reference files contain **no Mermaid diagrams** and reference **no images**. The repo's 260 `img/` files belong to the training/CUDA antilibrary chapters and are out of seam, so none are copied (per CONTEXT.md Target 6). Logged.
