# CATALOG — the books

The library index and status. For cross-cutting work items see [`BACKLOG.md`](BACKLOG.md); for
routing to a book see [`CONTEXT.md`](CONTEXT.md).

| Book | Status | Location | Notes |
|---|---|---|---|
| **Sans Python — Production AI Engineering** | ✅ Completed | [`library/completed/sans-python`](library/completed/sans-python/CONTEXT.md) | 8 modules shipped; `mdbook build` passes; M6/M7/M8 portfolio artifacts runnable. The reference book and the proof the engine works. |
| **Just Python** | ✅ Completed | [`library/completed/just-python`](library/completed/just-python/CONTEXT.md) | Applied Python (NumPy/Pandas/vectorization; 94% screen, gap-report row 1). **All 8 modules shipped** (M1 2026-06-20; M2–M8 2026-06-21). M2–M5: "NumPy in Depth", "Pandas for AI Pipelines", "Vectorization Discipline", "Python Idioms at Interview Speed" (each 5 lessons + 4 exercises). M6 + M7, the two portfolio modules: "Data Wrangling Artifact" (`wrangle.py`) and "Evaluation Engine" (`eval_engine.py`, per-class P/R/F1 in NumPy, no sklearn), each a runnable artifact + `pytest` smoke gate + skill write-up. **M8 "Integrated Python Engineering Exam", the capstone**: `pipeline.py` imports `wrangle.py` + `eval_engine.py` off disk and chains them (ingest → wrangle → merge → evaluate → integrated report) under a six-criterion `rubric.py` graded by code, with a `pytest` gate proving the locked oracle and a deficient-run failure. The reader ships one repo, three runnable scripts, a graded report. Reviewed to STYLE + STANDARDS, MS-Learn citations verified live, conductor ran the full reference harness green, `mdbook build` clean. Graduated to `library/completed/` 2026-06-21. |
| **Machine Math** | 🚧 In progress | [`library/in-progress/machine-math`](library/in-progress/machine-math/CONTEXT.md) | Graduated to in-progress 2026-06-21 (`GATE-NAME-BOOK` cleared: title **Machine Math**, slug `machine-math`). Classic ML paired with the math each fundamental needs (gap-report rows 3+6), applied depth; PyTorch/training split to Weights and Measures. **Module 1 "The Shape of Data" shipped 2026-06-21** (overview + 2 lessons: feature-vectors, knn-voting + 2 exercises; seeds the from-scratch `exercises/ml/` package throughline — `distances.py` + `knn.py`, each `pytest`-gated, negative case tested; `mdbook build` clean, reference harness 10/10, zero em-dashes, 7 MS-Learn citations verified live). Classic ML paired with the math each fundamental needs (gap-report rows 3+6); PyTorch/training split to Weights and Measures. Authored Haiku-fetch / Sonnet-author / Opus-conduct. M2–M7 running straight through (`GATE-APPROVE-SHIP` self-clear approved). Ore: `aefs` Phases 01–02 + `made-with-ml` eval pipeline. See `build-log/machine-math/build-progress.md`. |
| **Data Currents** | 🚧 In progress | [`library/in-progress/data-currents`](library/in-progress/data-currents/CONTEXT.md) | Graduated to in-progress 2026-06-21 (`GATE-NAME-BOOK` cleared: title **Data Currents**, slug `data-currents`). Interpreting + routing data for AI systems: SQL + the data platform beyond the Docling front door (gap-report row 4, 52%); extends Sans Python M5 L14. **Module 1 "SQL as an Operator Skill" shipped 2026-06-21** (overview + 4 lessons: your-telemetry-is-a-table, window-functions, ctes-and-the-diagnostic-chain, the-lineage-walk + 5 exercises; seeds the `module1-sql/` throughline — a SQLite telemetry store queried in DuckDB, `smoke.py` 8/8 + `pytest` 15/15 + negative-seed case; teach-warehouse-SQL/run-DuckDB; 6 MS-Learn citations verified live; `mdbook build` clean, zero em-dashes). **Module 2 "Batch Ingestion" shipped 2026-06-21** (overview + 4 lessons: the-incremental-merge, the-medallion-pattern, dbt-as-the-transform-layer, the-batch-freshness-slo + 4 exercises; throughline `module2-ingestion/` — a real dbt-duckdb medallion (bronze/silver/gold + 31 schema tests) + `ingest.py` idempotent MERGE keyed on (doc_id, content_hash) + `freshness_check.py` SLO gate; the M1 lineage walk runs unchanged against M2's gold tables and M2 writes the `corpus_loads` rows M1's freshness query reads — compounding proven; `dbt build` 37/37 + `smoke.py` 7/7 + `pytest` 39/39, negatives fail as designed; `mdbook build` clean). **Module 3 "Orchestration" shipped 2026-06-21** (overview + 4 lessons: the-dag-tasks-and-dependencies, retries-and-idempotency, scheduling-and-backfill, alerting-on-breach + 4 exercises; throughline `module3-orchestration/` — a Prefect 3.7.5 flow (fully offline) orchestrating the M2 pipeline as a DAG with retries, a nightly cron schedule, backfill, and an on-failure alert hook; reuses M2's `run_ingest` + `check_freshness`; teach-Airflow / run-Prefect; `smoke.py` 19/19 + `pytest` 23/23; `mdbook build` clean). Authored Haiku-fetch / Sonnet-author / Opus-conduct; the conductor locks the schema/oracle, a Sonnet artifact-engineer builds+tests the reference. M4–M8 self-clear approved (Ray 2026-06-21), authoring straight through. Ore: `made-with-ml` (M2–M4) + `ai-engineering-from-scratch` (M5–M6) + `aipe` (M5). See `build-log/data-currents/build-progress.md`. |
| **Weights and Measures** | 🚧 In progress | [`library/in-progress/weights-and-measures`](library/in-progress/weights-and-measures/CONTEXT.md) | Graduated to in-progress 2026-06-21 (`GATE-NAME-BOOK` cleared: title **Weights and Measures**, slug `weights-and-measures`). PyTorch + model training with quality as the throughline (gap-report row 2, 78%): the weights you train and the measures that prove them. **Module 1 "The PyTorch Operator" shipped 2026-06-21** (overview + 4 lessons: tensors-and-autograd, the-module-and-the-loop, data-optimizers-checkpoints, reading-a-real-train-py + 4 exercises; seeds the runnable spine `trainer.py` + the stdlib `check_loop.py` validator that gates the five-step loop + the trainable-param check, `--selftest` green; canonical code conductor-frozen on torch 2.12 CPU and verified real, e.g. grads `tensor(-3.) tensor(-1.)`, demo loss `1.1953 → 0.0534`; the from-scratch `aefs` code is fenced off as scaffolding, real PyTorch is what the reader writes; 7 doc anchors (PyTorch + MS-Learn Azure ML); `mdbook build` clean, zero em-dashes). **Module 2 "Fitting and Not Fitting" shipped 2026-06-21** (overview + 4 lessons: the-held-out-set, reading-loss-curves, early-stopping-as-a-policy, instrumenting-the-run + 4 exercises; extends the spine in place — `trainer.py` gains `evaluate()` + `fit()` early-stopping with best-checkpoint save/restore, `check_loop.py` gains `--module 2` gating the validation pass + early-stop signal, M1 byte-identical; the verdict-is-validation-loss thesis carried by a verified overfit demo (best epoch 4, early-stops at epoch 7); grounded in the in-book draft + `aefs` debugging + `made-with-ml` + the real Azure AI Foundry `results.csv` metrics; core torch has no built-in EarlyStopping so the `fit` hand-rolled pattern is the example; `mdbook build` clean, zero em-dashes). Authored Haiku-fetch / Sonnet-author / Opus-conduct (Opus designed the schema + ran the review gate, did NOT hand-code, per Ray's correction). M1–M2 PLANs locked; M3–M8 to author. Ore: `aefs` Phase 03 + `aipe` Ch 13 + `made-with-ml` training internals. See `build-log/weights-and-measures/m1/` + `m2/`. |
| **Answer Engineering** | ✅ Completed | [`library/completed/answer-engineering`](library/completed/answer-engineering/CONTEXT.md) | Graduated 2026-06-21 (`GATE-NAME-BOOK` cleared; was "Anatomy of an Answer"/`interview-algorithm`). The getting-hired book: answer construction as an engineering discipline — a transferable reasoning framework, not memorization. **All 8 modules (M1–M8) shipped 2026-06-21 — COMPLETE** (M1 "The Framework", overview + 2 lessons + 2 exercises, installs the four-step Algorithm + seeds the prep-dossier throughline; M2 "The Algorithm in Detail", overview + 4 lessons + 4 exercises, deepens each step on the hard cases + grows `check_prep.py` to v2 in place; M3 "Behavioral Interviews", overview + 4 lessons + 4 exercises, the behavioral example bank — each lesson reasons two worked examples to a STAR-L answer, two examples reused toward different signals to prove method-over-memorization, validator to v3; M4 "Technical Screens" (authored last), overview + 4 lessons + 4 exercises, the live-coding screen taught as the communication layer (clarify, narrate, debug out loud, test as you go), worked tasks grounded in the real Sans Python portfolio code so nothing is fabricated, ending in a weak-screen audit, validator to v8 (a sibling `--module 4` gate, not wired into the graded loop); M5 "AI/ML Systems-Design Interviews", overview + 4 lessons + 4 exercises, the systems-design bank — SPIDER as the construct motion, worked designs that reason from production constraints not reference architectures, a weak-design audit + "same prompt, different stress" anti-memorization coda, validator to v4, Haiku-fetch / Sonnet-author tier; M6 "The Sans Python Portfolio as Resume", overview + 4 lessons + 4 exercises, **Phase 3 opens** — walking an interviewer through a real artifact as a decision tour not a code reading, the same artifact narrated three ways per role, the written portfolio-narrative document + weak-walkthrough audit, worked examples are the real Sans Python portfolio artifacts, validator to v5; M7 "Live Practice and Calibration", overview + 4 lessons + 4 exercises, the practice engine — deliberate reps self-scored on the four Algorithm steps, **the self-review rubric realized as code** (`calibrate.py`, a calibration scorer with a readiness verdict, exit 0 = READY), the first throughline artifact that scores rather than just checks completeness, validator to v6; M8 "The Hiring Loop End-to-End", overview + 4 lessons + 4 exercises, the capstone — sequences the dossier across the five interview-loop stages and **grades it in code** via `grade_dossier.py` (composes `check_prep` + `calibrate`, exit 0 = READY), the Just Python M8 grader pattern applied to the career dossier, validator to v7). Reviewed to STYLE + STANDARDS, MS-Learn citations verified live where used, `mdbook build` clean. Ore: `asdg` interview half + the Sans Python portfolio. **M5 was authored before M4 (Ray-confirmed reorder); M4 was then authored last, reframed around the real Sans Python portfolio code, resolving the reorder.** **The book is COMPLETE: all 8 modules shipped, relocated to `library/completed/answer-engineering` 2026-06-21.** `GATE-PUBLISH` for a public copy remains open. |
| **Local Metal** | ✅ Completed | [`library/completed/local-metal`](library/completed/local-metal/CONTEXT.md) | The home production environment: build a homebrew rig (Cthulhu SPEC) and host local models to offload Claude Code. Progress-agnostic, home-scale companion to Sans Python; grows as the other books are ingested. **Graduated to in-progress 2026-06-21** (`GATE-NAME-BOOK` cleared: title **Local Metal**, slug `local-metal`). No hardware exists yet — "Cthulhu" is the build's codename, not a machine; the book is the buy-the-parts-to-managing-a-network curriculum, authored from grounded sources. **Module 1 "The Build" shipped 2026-06-21** (5 lessons + 4 exercises; the `HARDWARE.md` + `check_hardware.py` completeness-gated throughline plus `breakeven.py`; every hardware number grounded to a cited source, validator tested, `mdbook build` clean). **Module 2 "Linux and CUDA" shipped 2026-06-21** (5 lessons + 4 exercises; the `SETUP.md` + `check_setup.py` throughline on top of `HARDWARE.md` + `partition_plan.py`; MS-Learn + NVIDIA grounded, validator tested, build clean). **Module 3 "The Model Stack" shipped 2026-06-21** (5 lessons + 4 exercises; Ollama + Docker, `qwen2.5-coder:14b` on-card with a VRAM-Used ≤ VRAM-Total zero-overflow gate; throughline `MODELS.md` + `check_models.py` plus the runnable `ollama_client.py` OpenAI-compatible client that M5/M6 reuse; authored via a tiered fleet of 3 Haiku source-fetchers + 4 Sonnet authors; Ollama/Docker/NVIDIA/HF grounded, all code artifacts tested, build clean). **Module 4 "Unified Memory" shipped 2026-06-21** (5 lessons + 4 exercises; big models split across GPU VRAM + DDR5, the latency cost of the split, and the quantization tradeoffs that decide fit; throughline `LATENCY.md` + `check_latency.py` + standalone `bench.py` (tok/s + TTFT from `/api/generate` timing) + `vram_fit.py`; the aipe-grounded depth module: 14B on-card ~22-34 tok/s vs 32B Q4 split ~2-3 tok/s, 288 vs 96 GB/s bandwidth; tiered fleet of 3 Haiku fetchers + 4 Sonnet authors, all code tested, build clean). **Module 5 "The Routing Layer" shipped 2026-06-21** (5 lessons + 4 exercises; the first portfolio module: a local-vs-cloud router whose pure `route(request)` policy is offline `--selftest`-checkable and whose live arm wraps the M3 `ollama_client.py`; throughline `route.py` + `ROUTING.md` + `check_routing.py`; signals cost/latency/context/sensitivity with thresholds traced to the M4 baseline + Ollama/Claude context limits; tiered fleet of 2 Haiku fetchers + 4 Sonnet authors, all code tested, build clean). **Module 6 "Wire to Claude Code" shipped 2026-06-21** (5 lessons + 4 exercises; the portfolio capstone: a stdlib MCP stdio server (`mcp_server.py`, JSON-RPC 2.0, protocolVersion 2025-11-25) exposing an `ask_local` tool that wraps the M5 router + M3 client, registered with Claude Code via `.mcp.json`; throughline `mcp_server.py` + `.mcp.json` + `DELEGATION.md` + `check_delegation.py`; the runnable spine ollama_client.py → route.py → mcp_server.py reads end to end; server verified via `--selftest` AND real JSON-RPC over stdio; tiered fleet of 2 Haiku fetchers + 4 Sonnet authors, all code tested, build clean). **Module 7 "Perf and Tuning" shipped 2026-06-21** (5 lessons + 4 exercises; the aipe-depth capstone: profile the bandwidth-bound workload, apply the levers (flash attention, KV-cache quant, `OLLAMA_NUM_PARALLEL`, num_ctx), prove the gain; throughline `loadbench.py` (concurrent → aggregate tok/s + tail latency) + `TUNING.md` + `check_tuning.py` (tuned ≥ baseline gate); tiered fleet of 3 Haiku fetchers + 4 Sonnet authors, all code tested, build clean). **ALL 7 MODULES SHIPPED — content-complete: 35 lessons + 28 exercises, 7 portfolio docs (HARDWARE/SETUP/MODELS/LATENCY/ROUTING/DELEGATION/TUNING.md) + the runnable spine ollama_client.py → route.py → mcp_server.py, zero em-dashes book-wide, `mdbook build` clean.** **Relocated to `library/completed/local-metal` 2026-06-21** (the cataloguing move; route-manifest/router/route-lint updated). A public copy is the separate `GATE-PUBLISH`. Ore: `aipe` + the Cthulhu SPEC. |

> **Dual-use + blueprint.** Every book is written to be read by a human learner *and* ingested by an LLM
> — dense, linked, plain markdown; the same page serves either party. **Sans Python is the core roadmap
> and blueprint**; the planned books branch from its
> [antilibrary gap report](build-log/sans-python/antilibrary-gap-report.md) (the retired "Avec Python"
> umbrella, now split into focused books).

## Status legend

- ✅ **Completed** — shipped; all stages passed; lives under `library/completed/`.
- 🚧 **In progress** — actively authoring; lives under `library/in-progress/`.
- 📋 **Planned** — dossier/ore identified, not yet started; lives under `library/planned/`. Starting
  one is gated at `GATE-NAME-BOOK` (see [`platform/HUMAN-GATES.md`](platform/HUMAN-GATES.md)).

## How a book moves

`planned/` → (`GATE-NAME-BOOK`) → `in-progress/` → author via the
[pipeline](platform/pipeline/CONTEXT.md) (`GATE-LOCK-PLAN`, `GATE-APPROVE-SHIP` per stage) →
`completed/`. Publishing a public copy is a separate decision gated at `GATE-PUBLISH`.
