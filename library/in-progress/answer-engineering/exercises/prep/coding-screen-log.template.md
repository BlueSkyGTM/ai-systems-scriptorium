# Coding-Screen Log

Copy this file to `coding-screen-log.md` and log reps across the four Module 4 exercises:
narrating-the-screen, the-happy-path-and-the-edges, debugging-out-loud,
the-clock-and-the-weak-screen-audit.

Each rep is a ## Screen <n> entry. Work through the exercises in order; by the end
at least three entries must be present and `python check_prep.py --module 4` must exit 0.

---

## Screen 1

**Task:** <fill in: the task you attempted, e.g. "Implement a retrieval relevance floor that drops results below a min_score threshold">

**Clarifying questions:** <fill in: the questions you asked before coding, e.g. "What should the function return when no results clear the floor?">

**Approach narration:** <fill in: the reasoning you narrated as you coded, e.g. "I started with the data shape, then the filter, then the edge cases">

**Stuck and recovery:** <fill in: a point where you got stuck and what you did, or "none" if you were not stuck>

**Test cases:** <fill in: the edge cases you wrote tests for, e.g. "empty input, all below floor, exactly at floor, mixed above and below">

**Verdict:** <fill in: what the rep revealed -- one specific gap or confirmation to carry forward>
