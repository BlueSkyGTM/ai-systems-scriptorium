# Hyperparameters and the Tradeoff

Zero training error is a warning, not a win.

A gradient boosting model has three knobs that matter: how many trees you build, how much each
tree contributes, and how deep each tree grows. Turn them all the way up and the model drives
training error to zero. Turn them up too far and the model memorizes noise, and test error
climbs. This is not a new story: you measured it with polynomial degree in M2, with `max_depth`
in M3, and with `k` in M1. Gradient boosting gives you three knobs instead of one, and getting
any of them wrong overfits even a strong model.

## The Experiment

The dataset is the Friedman-1 regression benchmark: 300 samples, 10 features, the true signal
coming from only five of them. Split 80/20. Train `ml.ensemble.GradientBoostingRegressor` at
two configurations and measure train MSE and test MSE.

| Config | Final Train MSE | Test MSE | Test R^2 |
|--------|-----------------|----------|----------|
| Tuned (n=50, lr=0.05, depth=3) | 3.06 | 7.29 | 0.665 |
| Overfit (n=200, lr=0.5, depth=8) | 0.00 | 11.52 | 0.471 |

The overfit configuration drives training error to zero: deep trees, a large learning rate, and
many rounds combine to fit every quirk in the training data. Test error is 58% worse than the
tuned configuration. More capacity is not better.

## The Three Knobs

**`n_estimators`: rounds of boosting.** Each round adds one more tree that corrects the
residuals left by the previous ensemble. More rounds mean more capacity. Azure Machine Learning's
Boosted Decision Tree Regression component exposes this as "Number of trees constructed," and
its documentation states directly that "by creating more decision trees, you can potentially get
better coverage, but training time increases" ([learn.microsoft.com/azure/machine-learning/component-reference/boosted-decision-tree-regression](https://learn.microsoft.com/en-us/azure/machine-learning/component-reference/boosted-decision-tree-regression?view=azureml-api-2)).
With a large learning rate, each tree is trusted fully, so a few hundred rounds can saturate the
training set. With early stopping, you can let `n_estimators` run high and halt when the
validation error stops improving.

**`learning_rate`: shrinkage.** Each tree's output is multiplied by this factor before being
added to the ensemble. A smaller factor means each tree contributes less, so the ensemble needs
more rounds to fit the same signal. The Azure ML component's parameter description captures the
tradeoff: "if the step size is too big, you might overshoot the optimal solution; if the step
size is too small, training takes longer to converge on the best solution" ([learn.microsoft.com/azure/machine-learning/component-reference/boosted-decision-tree-regression](https://learn.microsoft.com/en-us/azure/machine-learning/component-reference/boosted-decision-tree-regression?view=azureml-api-2)).
In practice, small `learning_rate` paired with more `n_estimators` generalizes better than a
large rate with few rounds: both can hit similar training error, but the slow learner smooths out
the path to the solution. The source data (`aefs-ensembles.md`) records this explicitly: configs
at lr=0.5/n=20, lr=0.1/n=100, lr=0.05/n=200, and lr=0.01/n=500 all converge to similar test
MSE, confirming that `learning_rate` and `n_estimators` are a coupled pair, not two independent
choices.

**`max_depth`: tree depth.** This is the same knob you controlled in M3, now applied to each
base learner in the ensemble. Gradient boosting works best with shallow trees: the ingredient
source notes a default range of 3 to 8 for boosting, much shallower than standalone trees.
A depth-3 tree can capture up to three-way feature interactions. A depth-8 tree can fit 256
distinct leaf patterns per round: at 200 rounds, the overfit configuration has enormous capacity.
The Azure ML component uses "Maximum number of leaves per tree" to control the same thing,
and warns that "by increasing this value, you potentially increase the size of the tree and get
better precision, at the risk of overfitting and longer training time."

## Why Three Knobs Overfit Together

The table tells the story cleanly. The overfit configuration does not fail at one knob: it fails
at all three simultaneously. A large learning rate means every tree is trusted in full. Deep
trees give each tree high variance on its own. Many rounds let that variance accumulate over 200
iterations. The result is a training error of 0.00, which means the ensemble has fit not just
the signal but every residual, including the ones generated by the noise term.

The tuned configuration restricts all three: shallower trees (depth=3), smaller learning rate
(0.05, trusting each tree at 5% of its output), and fewer rounds (50). It leaves some training
error on the table (3.06 final train MSE) because it is not trying to fit the noise. That
restraint pays off: 7.29 test MSE versus 11.52.

Azure Machine Learning's Automated ML documentation describes the same pattern at the system
level. Its guidance on preventing overfitting notes that "Automated ML also implements explicit
model complexity limitations to prevent overfitting. In most cases, this implementation is
specifically for decision tree or forest algorithms. Individual tree max-depth is limited, and
the total number of trees used in forest or ensemble techniques are limited" ([learn.microsoft.com/azure/machine-learning/concept-manage-ml-pitfalls](https://learn.microsoft.com/en-us/azure/machine-learning/concept-manage-ml-pitfalls?view=azureml-api-2)).
AutoML is enforcing the same discipline by policy that you must enforce by hand.

## The Thread Running Through M1 to M4

Every module has introduced one version of the bias-variance tradeoff:

- **M1**: `k` in k-NN. At `k=1`, the boundary traces every training point: high variance. At
  large `k`, it smooths to a broad region: high bias.
- **M2**: Polynomial degree. Degree 1 underfits the curve: high bias. Degree 10 fits the noise:
  high variance.
- **M3**: `max_depth` in a single decision tree. No limit: 1.000 train accuracy, noise
  memorized.
- **M4**: Three coupled knobs in gradient boosting. Any one of them turned up too far, or all
  three together, produces the same signature: training error falls, test error climbs.

The parameter changes across modules. The tradeoff does not.

## What This Means at Scale

XGBoost and LightGBM expose the same three parameters under slightly different names. At
production scale, the standard practice is to fix a small `learning_rate` (0.01 to 0.05), set
`n_estimators` high enough that it would not be the binding constraint, and use early stopping
against a held-out validation set to find the actual stopping round. Then `max_depth` (or
`num_leaves` in LightGBM) is swept by cross-validation. The configuration that minimizes
validation error is the one that ships. This is a cross-validation search over a coupled
parameter space: the same search you will write in the exercise.

The hiring test for this is not naming the parameters. It is explaining what happens to train
error and test error when you change them, and knowing which direction each knob should move
when the model is overfitting.

## Core Concepts

- `n_estimators`, `learning_rate`, and `max_depth` are coupled: large learning rate trusts each
  tree fully, deep trees make each tree high-variance, and many rounds let both effects compound
  into a model that memorizes the training set.
- Shrinkage (`learning_rate`) and `n_estimators` are a matched pair: a smaller rate needs more
  rounds to fit the same signal, but the slower path generalizes better because it averages over
  more small corrections rather than committing to a few large ones.
- The overfit signature is a number: the experiment above shows a training MSE of 0.00 and a
  test MSE of 11.52 versus 7.29 for the tuned config, a 58% penalty for extra capacity.
- This is the same bias-variance tradeoff from M1 (k-NN `k`), M2 (polynomial degree), and M3
  (tree `max_depth`): the algorithm changes, the tradeoff does not.

<div class="claude-handoff" data-exercise="exercises/module4/hyperparameters-and-the-tradeoff/">

**Build It in Claude Code**: Write the hyperparameter sweep and its gate at
`exercises/module4/hyperparameters-and-the-tradeoff/`. Use `ml.ensemble.GradientBoostingRegressor`
on the Friedman-1 benchmark to show that a high-variance configuration (large `learning_rate`,
many deep estimators) overfits relative to a tuned configuration: lower training error, but
worse test error. The gate at
`exercises/module4/hyperparameters-and-the-tradeoff/test_tradeoff.py` must pass green with
`python -m pytest exercises/module4/hyperparameters-and-the-tradeoff`.

</div>
