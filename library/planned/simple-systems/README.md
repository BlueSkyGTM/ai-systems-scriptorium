# Simple Systems (planned)

A planned book on AI system design that starts simple and earns every layer — draw the smallest
system that works, then add only what the load demands. The antidote to architecture-by-default.

**Status:** planned. Ore identified, not yet distilled. Start gated at `GATE-NAME-BOOK`.

**Ore:** `vault/ai-system-design-guide` — the production-reference / design-patterns half (see
[`vault/MANIFEST.md`](../../../vault/MANIFEST.md)).

To work here, read [`CONTEXT.md`](CONTEXT.md) first.
