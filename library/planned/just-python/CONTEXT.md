# CONTEXT — Just Python (PLANNED)

A planned book covering the **Python gaps** *Sans Python* deliberately left at point-of-use: the applied
Python an AI Engineer is screened on but the core book never drills — NumPy, Pandas, vectorized ops, the
idioms behind the **94%-of-postings "Python" screen**. Where Sans Python teaches AI engineering *without
making Python the subject*, Just Python is the focused complement that makes it the subject.

Sans Python stays the **core roadmap and blueprint**; this branches from it. Origin: row 1 — the biggest
gap — of `build-log/sans-python/antilibrary-gap-report.md`. (Replaces the retired "Avec Python" umbrella,
now split into focused books.)

**Not started.** Starting it is gated at `GATE-NAME-BOOK` (propose the real title first).

## Ore (in the vault — not yet distilled)

- Applied-Python material across the vault — e.g. `made-with-ml` data/code chapters and the
  `ai-engineering-from-scratch` Python foundations. Survey at process-ore time via `vault/MANIFEST.md`.

## Dual-use

Like every book here, written to be **read by a human learner and ingested by an LLM** — dense, linked,
plain markdown. The same page serves either party; that is part of why the focused books pay off.

## Load / don't-load

- **Load (when it graduates):** this folder's `README.md`, the named vault ore via `vault/MANIFEST.md`,
  `ingredients`, `platform/pipeline`.
- **Do NOT load:** the shipped book, other planned books.

## Handoff & gates

`GATE-NAME-BOOK` → process ore via `vault/CONTEXT.md` → `platform/pipeline/CONTEXT.md`
(`GATE-LOCK-PLAN`, `GATE-APPROVE-SHIP`). See `platform/HUMAN-GATES.md`.
