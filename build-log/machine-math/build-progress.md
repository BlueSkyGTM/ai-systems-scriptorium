# Machine Math — Build Progress

Per-stage authoring status. One row per module.

| Module | Title | Status | Shipped | Notes |
|--------|-------|--------|---------|-------|
| M1 | The Shape of Data | ✅ Shipped | 2026-06-21 | Overview + 2 lessons (feature-vectors, knn-voting) + 2 exercises. Installs the book's core reframing (a row is a point in feature space) by pairing the linear-algebra primitives (vectors, the dot product, Euclidean/cosine/Manhattan distance, min-max + z-score scaling) with k-nearest neighbors, the first algorithm that is pure geometry. Seeds the **from-scratch `exercises/ml/` package** throughline: `ml/distances.py` (three metrics + two scalers + the `METRICS` dispatch table) and `ml/knn.py` (the lazy `KNNClassifier` that imports `METRICS`, real reuse). Each exercise ships a `pytest` acceptance gate; the negative case is tested (a feature blown up by 1000x sinks an unscaled vote, z-scoring recovers it: 0.800 -> 1.000). Ships `exercises/CLAUDE.md`. First module of this book, so it also distilled the book's first ingredients from raw `aefs` ore (`ingredients/source/machine-math/`) and stood up the mdBook scaffold. Plan: `m1/PLAN.md`. |
| M2 | Fitting a Line (and Its Limits) | ✅ Shipped | 2026-06-21 | Overview + 4 lessons (gradient-descent, linear-regression, logistic-regression, bias-variance) + 4 exercises. The shift from storing data (M1) to fitting parameters by minimizing a loss. Adds `ml/gradient_descent.py` (the shared optimizer), `ml/linreg.py`, `ml/logreg.py` to the throughline package; linreg/logreg both reuse `gradient_descent` and M1's `zscore` (real reuse, no optimizer copy). Measured + cited: GD on (theta-3)^2 -> 3.0; linreg R^2 0.9966 identical to sklearn AND the normal equation; logreg 96.49% test acc / 99.12% sklearn agreement; bias-variance shown by a degree sweep (test MSE 0.61 at deg 1 -> 4.79 at deg 15 while train falls, the negative case). Sonnet builder built + tested the code (13/13 `pytest`), Opus ran the gate; 2 Sonnet authors wrote lessons + exercises. `mdbook build` clean, zero em-dashes, MS-Learn citations verified live (Azure ML Linear Regression / Two-Class Logistic Regression components). Plan: `m2/PLAN.md`. |
| M3 | Splitting and Branching | ✅ Shipped | 2026-06-21 | Overview + 3 lessons (entropy-and-information-gain, decision-trees, overfitting-and-cross-validation) + 3 exercises. From distance (M1) and parametric fitting (M2) to the third question: which yes/no split best separates the labels? Adds `ml/tree.py` (`entropy`/`gini`/`information_gain` + a from-scratch `DecisionTreeClassifier`, greedy recursive best-split, reuses the impurity functions). Measured + cited: entropy 0 (pure) / 1.0 bit (balanced 2-class) / 2.0 bits (4-class); Gini 6/4 = 0.48; perfect-split gain 0.5 Gini / 1.0 bit; Iris tree 0.9667 = sklearn, 30/30 agreement; overfitting via a depth sweep on breast cancer (train -> 1.000 by depth 7, test peaks at depth 3 = 0.951 then settles 0.944, the negative case) + cross-validation as the depth-picker (callback to M2 degree + M1 k as the same bias-variance knob). Sonnet builder built + tested (13/13 `pytest`), Opus ran the gate; 2 Sonnet authors wrote lessons + exercises. `mdbook build` clean, zero em-dashes, MS-Learn verified live (Two-Class/Multiclass Decision Forest, Cross Validate Model, manage-ml-pitfalls). Plan: `m3/PLAN.md`. |
| M4 | Ensembles and the Gradient | ✅ Shipped | 2026-06-21 | Overview + 3 lessons (bagging-and-random-forests, boosting-and-the-gradient, hyperparameters-and-the-tradeoff) + 3 exercises. One tree is high-variance; combine many. Adds `DecisionTreeRegressor` to `ml/tree.py` and `ml/ensemble.py` (`RandomForestClassifier` bags M3 trees; `GradientBoostingRegressor` fits trees to residuals, which for squared loss is the negative gradient -> gradient descent in function space, callback to M2). Measured + cited: single tree 0.9123 -> 100-tree forest 0.9386 (breast cancer), 99.12% sklearn agreement; GB test R^2 0.9038 vs sklearn 0.9045, train MSE 22.6 -> 0.47 monotone across rounds, 0.9999 prediction correlation; hyperparameter overfit sweep (tuned R^2 0.665 vs overfit 0.471 with train -> 0, the negative case). Sonnet builder built + tested (11/11 `pytest`), Opus ran the gate; 2 Sonnet authors wrote lessons + exercises. `mdbook build` clean, zero em-dashes, MS-Learn verified live (Boosted Decision Tree / Decision Forest Regression / Two-Class Boosted Decision Tree). Plan: `m4/PLAN.md`. |
| M5 | What "Good" Means | ✅ Shipped | 2026-06-21 | Overview + 3 lessons (confusion-matrix-and-thresholds, auc-roc-the-math-of-ranking, regression-error-and-slicing) + 3 exercises. Whether a model is good, and good at what. Adds `ml/metrics.py` (from scratch, NO sklearn inside): confusion_matrix / precision / recall / f1 / accuracy, roc_curve / roc_auc (Mann-Whitney rank form), mae / rmse / r2, slice_metric. **This is the evaluation library the M7 capstone composes.** Measured + cited: worked confusion matrix (acc 0.60, P/R/F1 0.667); imbalance case (acc 0.95 while minority recall 0.0, accuracy lies); roc_auc matches sklearn to floating-point zero; RMSE penalizes one injected outlier 38.3x vs MAE 12.5x; a slice table where aggregate MAE 1.08 hides a subgroup at 3.83 (3.55x worse, the failure-mode the aggregate hid). Sonnet builder built + tested (22/22 `pytest`), Opus ran the gate; 2 Sonnet authors wrote lessons + exercises. `mdbook build` clean, zero em-dashes, MS-Learn verified live (Evaluate Model, AutoML evaluation results, error-analysis cohorts). Plan: `m5/PLAN.md`. |
| M6 | Feature Reality | ✅ Shipped | 2026-06-21 | Overview + 4 lessons (scaling-and-encoding, naive-bayes, imbalanced-data, feature-selection) + 4 exercises. The unglamorous work that decides whether the math works. Adds `ml/features.py` (one_hot_encode/ordinal_encode, random_oversample/undersample, class_weights, select_by_variance, select_by_importance via M4 forest permutation importance; reuses M1 scalers) and `ml/naive_bayes.py` (GaussianNB, log-posterior). Measured + cited: encoders match sklearn; GaussianNB Iris 0.9667 = sklearn (30/30); imbalance 95/5 raw minority recall 0.0 -> 0.4545 after oversample (callback to M5 accuracy-lies); selection drops the injected constant and keeps the single signal column (DT 0.975 on 8 feats vs 0.983 on the selected one), callback to M1 curse of dimensionality. Sonnet builder built + tested (21/21 `pytest`), Opus ran the gate; 2 Sonnet authors wrote lessons + exercises. `mdbook build` clean, zero em-dashes, MS-Learn verified live (featurization/Normalize Data, SMOTE, Filter-Based + Permutation Feature Importance). Plan: `m6/PLAN.md`. |
| M7 | The Portfolio Artifact | ✅ Shipped | 2026-06-21 | **The capstone.** Overview + 4 lessons (the-dataset-and-the-feature-pipeline, training-the-model, honest-evaluation-and-slicing, the-model-card-and-the-rubric) + 4 exercises. `pipeline.py` COMPOSES `ml.features` + `ml.metrics` OFF DISK on a real tabular dataset (seeded synthetic churn benchmark, 1000 rows, ~30% positive, numeric + categorical + region subgroup): engineer features with the reader's `ml.features`, train a sklearn `GradientBoostingClassifier`, evaluate with the reader's `ml.metrics` (AUC 0.887 / F1 0.745 / P 0.837 / R 0.672), slice by region (worst subgroup east at 0.60 recall), write `MODEL_CARD.md`. `rubric.py` grades the run on six criteria in code (R1 RUNS, R2 FEATURES-ENGINEERED, R3 EVALUATED, R4 SLICED, R5 COMPOSED, R6 MODEL-CARD) = the same six in the lesson prose (one rubric in two forms); good run 6/6, three deficient runs each fail exactly the criterion they offend (no_encoding->R2, no_slicing->R4, stub_card->R6). Sonnet builder built + tested (27/27 `pytest`), Opus ran the gate + fixed the shared-card-path bug + the class-balance prose; 2 Sonnet authors wrote lessons + exercises. `mdbook build` clean, zero em-dashes book-wide, MS-Learn verified live (ML pipelines, Responsible AI scorecard, error-analysis cohorts). Plan: `m7/PLAN.md`. **BOOK CONTENT-COMPLETE: 7/7 modules, 23 lessons + 7 overviews + 23 exercises, the 10-module from-scratch `ml/` package + the composing capstone.** |

## Provenance

M1 graduated the book through `GATE-NAME-BOOK` (title **Machine Math**, slug `machine-math`, chosen
2026-06-21 from the blueprint's lead candidate) and ran the full AUTHOR → VERIFY → BUILD/TEST → SHIP
cycle. `GATE-LOCK-PLAN` was cleared by Ray's "go" against the pre-vetted Module 1 draft (low
wasted-work risk); `GATE-APPROVE-SHIP` was presented and approved before the commit. Ray also approved
self-clearing `GATE-APPROVE-SHIP` for M2–M7 to run the book straight through.

Authored under the standing division of labor (Ray's 2026-06-21 mandate, corrected mid-M1): **Haiku
fetch tier → Sonnet authors → Opus conducts and gates.** Two Haiku workers distilled the raw `aefs`
math/ML ore (`vault/ai-engineering-from-scratch/phases/01-math-foundations` + `02-ml-fundamentals`)
into `ingredients/source/machine-math/`. Three Sonnet workers authored the two lessons and finished the
overview, exercises, and coaching contract, using their coding tools. Opus locked the M1 plan and the
artifact contract (the `ml/` package signatures + the `pytest` gate shape), dispatched, reviewed every
draft against STYLE + STANDARDS, and ran the gate. (Opus also hand-built the initial `ml/` reference
harness before Ray's "no excessive coding on your part" correction; that tested reference now lives in
`m1/output/author/ref/` as the answer key, and code authoring moved to the Sonnets thereafter.)

VERIFY + BUILD/TEST results at ship: `mdbook build` clean; the reference `pytest` harness 10/10 green;
zero em-dashes book-wide; 7 Microsoft Learn citations live-fetched and verified (4 in feature-vectors,
3 in knn-voting); `route-lint` green. The from-scratch dependency policy held: NumPy for all algorithms,
scikit-learn only as the dataset source and the test oracle.
