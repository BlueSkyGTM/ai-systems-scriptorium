# Local Metal — Build Progress

Per-stage authoring status. One row per module. Seven modules: six track the Cthulhu SPEC's
six How-To steps, plus a perf-and-tuning capstone (M7). See
`library/completed/local-metal/README.md` for the full blueprint.

| Module | Title | Status | Shipped | Notes |
|--------|-------|--------|---------|-------|
| M1 | The Build | ✅ Authored, verified, built — awaiting `GATE-APPROVE-SHIP` | — | 5 lessons (overview + the-cost-wall-and-the-rig, why-each-part, the-micro-center-run, document-your-build) + 4 exercises. Throughline: `HARDWARE.md` + `check_hardware.py` built across L2–L4; `breakeven.py` standalone in L1. Authored conductor-direct (4 Sonnet workers; conductor wrote the preface/overview, reviewed every draft, reconciled a 3-way BOM-column divergence to one 5-column layout, and tested the validator: 1 positive + 4 negative cases pass). VERIFY: STYLE clean (zero em-dashes); every hardware number grounded to a real cited URL (no fabrication), captured to `vault/local-metal/m1-sources/PROVENANCE.md`. BUILD: `mdbook build` clean. Plan: `m1/PLAN.md`. |
| M2 | Linux and CUDA | ✅ Authored, verified, built — awaiting `GATE-APPROVE-SHIP` | — | 5 lessons (overview + the-dual-partition-plan, choosing-and-installing-linux, nvidia-drivers-and-cuda, verify-the-gpu-is-visible) + 4 exercises. Throughline: `SETUP.md` + `check_setup.py` built across L2–L4 (README-only; the repo's 2nd file, metal → OS → stack); `partition_plan.py` standalone in L1. Authored conductor-direct (4 Sonnet workers). VERIFY: STYLE clean (zero em-dashes); MS-Learn-primary grounding (WSL2 contrast) + NVIDIA/Ubuntu docs, cited URLs captured to `vault/local-metal/m2-sources/PROVENANCE.md`. Conductor reconciled an L4 schema divergence (off-spec field names → canonical Distro/Version/Kernel/Disk layout + 3 driver fields) and an L2↔L3 function-naming mismatch; final validator tested (1 positive + 4 negative cases). BUILD: `mdbook build` clean. Plan: `m2/PLAN.md`. |
| M3 | The Model Stack | ✅ Authored, verified, built — shipped `GATE-APPROVE-SHIP` | 2026-06-21 | 5 lessons (overview + install-ollama-and-docker, a-14b-model-on-card, the-openai-compatible-api, record-the-stack) + 4 exercises. Throughline: `MODELS.md` + `check_models.py` (the 3rd portfolio file after HARDWARE.md/SETUP.md; VRAM-Used ≤ VRAM-Total zero-overflow gate enforced as code) + `ollama_client.py` (the runnable M5/M6 seed; OpenAI-compatible, `--selftest` offline) + standalone `stack_check.py` (L1). Authored conductor-direct with a new tiered fleet: **3 Haiku source-fetchers** (Ollama/Docker/NVIDIA + quant grounding) feeding **4 Sonnet lesson authors**; conductor wrote the overview, owned + tested all 4 code artifacts (byte-identical to what ships), reconciled a nested-fence rendering bug in L4 (3-backtick → 4-backtick outer fence in both Step-1 snippets). VERIFY: STYLE clean (0 em-dashes); grounded to real Ollama/Docker/NVIDIA/HF URLs, captured to `vault/local-metal/m3-sources/PROVENANCE.md`. BUILD/TEST: `mdbook build` clean; shipped `check_models.py` (1 positive + overflow + blank-field), `ollama_client.py --selftest`, `stack_check.py --selftest` all pass. Plan: `m3/PLAN.md`. |
| M4 | Unified Memory | ✅ Authored, verified, built — shipped `GATE-APPROVE-SHIP` | 2026-06-21 | 5 lessons (overview + when-a-model-doesnt-fit, quantization-tradeoffs, measure-latency, record-the-baseline) + 4 exercises. Throughline: `LATENCY.md` + `check_latency.py` (4th portfolio file; TTFT/tok-s positive-measurement gate) + standalone `bench.py` (reads `/api/generate` timing → tok/s + TTFT; `--selftest` offline) + `vram_fit.py` (L2 planner). The aipe-grounded depth module: 14B on-card (~22-34 tok/s) vs 32B Q4 split (~2-3 tok/s, real 52% GPU / 48% CPU); 288 vs 96 GB/s bandwidth gap; Q4/Q8/F16 size+quality. Authored conductor-direct tiered fleet: **3 Haiku source-fetchers** (Ollama offload/timing; quant+bandwidth incl. local aipe ore survey; big-model sizes + real splits) feeding **4 Sonnet authors**; conductor wrote the overview, owned + tested all code artifacts (byte-identical to ship), reconciled L3's representative tok/s down to grounded reality. VERIFY: STYLE clean (0 em-dashes); grounded to Ollama/llama.cpp/NVIDIA/vendor + community benchmarks, captured to `vault/local-metal/m4-sources/PROVENANCE.md`. BUILD/TEST: `mdbook build` clean; shipped `check_latency.py` (positive + zero-toks), `bench.py --selftest`, `vram_fit.py` (both documented cases) all pass. Plan: `m4/PLAN.md`. |
| M5 | The Routing Layer | ✅ Authored, verified, built — shipped `GATE-APPROVE-SHIP` | 2026-06-21 | 5 lessons (overview + the-routing-decision, signals-and-thresholds, build-the-router, record-the-policy) + 4 exercises. First portfolio module: `route.py` (pure `route(request)` policy, `--selftest`-checkable offline; live `--run` arm imports + calls `ollama_client.py` from M3, so the M3 work compounds) + `ROUTING.md` (5th portfolio file) + `check_routing.py` (3-section + ≥3-signal-rows + written-policy gate). Routing signals cost/latency/context/sensitivity grounded; privacy framed honestly as a local-first rule (not over-claimed). Thresholds (8K local context, 2000 ms) traced to M4 baseline + Ollama/Claude context limits. Authored conductor-direct tiered fleet: **2 Haiku source-fetchers** (routing patterns; context/pricing) feeding **4 Sonnet authors**; conductor wrote overview, owned + tested route.py + check_routing.py (byte-identical to ship; ROUTING.md avoids `<`/`>` so the placeholder guard stays intact). VERIFY: 0 em-dashes; grounded to LiteLLM/Ollama/Anthropic + engineering write-ups, captured to `vault/local-metal/m5-sources/PROVENANCE.md`. BUILD/TEST: `mdbook build` clean; route.py --selftest (5/5), check_routing.py (positive + min-signals negative) all pass. Plan: `m5/PLAN.md`. |
| M6 | Wire to Claude Code | ✅ Authored, verified, built — shipped `GATE-APPROVE-SHIP` | 2026-06-21 | 5 lessons (overview + mcp-in-one-page, build-the-mcp-server, register-with-claude-code, log-the-delegation) + 4 exercises. The portfolio capstone: a stdlib MCP stdio server (`mcp_server.py`, JSON-RPC 2.0, protocolVersion 2025-11-25; initialize/tools/list/tools/call) exposing an `ask_local` tool that wraps the M5 `route.py` (decision) + M3 `ollama_client.py` (local call); `.mcp.json` (project-scope stdio registration) + `claude mcp add`; `DELEGATION.md` (6th portfolio file, the end-to-end proof) + `check_delegation.py` (sections + real local/cloud route + non-empty reply gate). The runnable spine ollama_client.py -> route.py -> mcp_server.py reads end to end. Approach: MCP delegation tool (not full model-swap proxy / Agent SDK). Authored conductor-direct tiered fleet: **2 Haiku source-fetchers** (MCP spec; Claude Code MCP registration) feeding **4 Sonnet authors**; conductor owned + tested all artifacts (byte-identical to ship) and verified the server BOTH via `--selftest` AND by piping real JSON-RPC over stdio (spec-correct responses). VERIFY: 0 em-dashes; grounded to the MCP spec + Claude Code docs, captured to `vault/local-metal/m6-sources/PROVENANCE.md`. BUILD/TEST: `mdbook build` clean; mcp_server.py --selftest + live stdio, check_delegation.py (positive + bad-route negative) all pass. Plan: `m6/PLAN.md`. |
| M7 | Perf and Tuning | ✅ Authored, verified, built — shipped `GATE-APPROVE-SHIP` | 2026-06-21 | 5 lessons (overview + where-the-time-goes, the-tuning-levers, throughput-under-load, record-the-tuning) + 4 exercises. The aipe-depth capstone (final module): profile the bandwidth-bound workload (`nvidia-smi dmon -s um`, high mem% + sm% not pegged), apply the levers (flash attention, `OLLAMA_KV_CACHE_TYPE` q8_0/q4_0, `OLLAMA_NUM_PARALLEL`, num_ctx), and prove the gain. Throughline: `loadbench.py` (concurrent load → aggregate tok/s + tail latency; `--selftest` offline) + `TUNING.md` (7th portfolio file, baseline-vs-tuned + levers) + `check_tuning.py` (tuned ≥ baseline non-regression gate). Headline before/after: num_parallel raises aggregate throughput under load (representative 30 → 64 tok/s). Authored conductor-direct tiered fleet: **3 Haiku source-fetchers** (Ollama perf env vars; nvidia-smi profiling + 4060 Ti numbers; local aipe ore survey) feeding **4 Sonnet authors**; conductor owned + tested all code (byte-identical to ship). VERIFY: 0 em-dashes; grounded to Ollama/NVIDIA docs + benchmarks + aipe ore (ch15/16/19, roofline), captured to `vault/local-metal/m7-sources/PROVENANCE.md`. BUILD/TEST: `mdbook build` clean; loadbench.py --selftest, check_tuning.py (positive + regression negative) all pass. Plan: `m7/PLAN.md`. |

## Provenance

Graduated to in-progress on 2026-06-21 (`GATE-NAME-BOOK` cleared: title **Local Metal**, slug
`local-metal`), then relocated to `library/completed/local-metal` on content-completion 2026-06-21
(all 7 modules shipped; the cataloguing move, no gate). **No hardware exists** — "Cthulhu" is the codename for the
reference build (the SPEC's BOM), not a running machine; the book is the curriculum that takes a
reader from buying the parts to managing a networked local-model host. This is the seventh book
and the library's only hardware-grounded title — the place every other book eventually comes to
run. Unlike Just Python's code exercises (pytest-checkable scripts), Local Metal's done-whens are
structured-document validators plus representative **published** command output (`nvidia-smi`,
`ollama`, latency logs) the reader reproduces on their own build; the M1 plan settles how the
hardware modules meet the STANDARDS "machine-checkable done-when" bar without a live rig.
