# CONTEXT — Everything Else (antilibrary submissions) — IN PROGRESS

The companion book to *Sans Python*: the **antilibrary submissions** — the "Avec Python" candidates
and everything the shipped book deliberately cut, written up as its own collection. Working title
"Everything Else"; the real title is set at `GATE-NAME-BOOK` if/when it graduates.

## Seed source

Start from the shipped antilibrary page: `library/completed/sans-python/src/antilibrary.md`. Each
entry there (what a module cut and why, the Avec Python list) is a candidate chapter here.

## Load / don't-load

- **Load:** `library/completed/sans-python/src/antilibrary.md` (the seed), this folder's `README.md`,
  `platform/conventions` (voice), `platform/pipeline` (when authoring).
- **Do NOT load:** the full shipped book, the `vault` raw ore (distill via `ingredients` first),
  other planned books.

## Handoff & gates

Author via `platform/pipeline/CONTEXT.md`. Gates by ID (see `platform/HUMAN-GATES.md`):
`GATE-NAME-BOOK` (set the real title), `GATE-LOCK-PLAN`, `GATE-APPROVE-SHIP`.
