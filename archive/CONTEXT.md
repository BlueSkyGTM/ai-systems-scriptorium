# CONTEXT — archive/ (HISTORICAL — not active)

**HISTORICAL — not active. Do NOT route here.** The root task router never targets this folder, and
`route-lint` fails the build if any live route (including a markdown body link in a routed dir) points
into `archive/`.

## What's here

Retired scaffolding from before the restructure: the old "Workspace Builder" root `CLAUDE.md`, the
old root `CONTEXT.md`, the `northstar/` planning shell (CONTEXT/HANDOFF/SPEC/SYLLABUS/REFERENCE-LAYER),
the old `setup/` `stages/` `references/` trees, and synthesis byproducts.

## Why it's kept

Provenance and recoverability, nothing more. If you need to know how the repo used to be framed, it's
here. Nothing in here is current; nothing here should be loaded for active work.

This folder is route-excluded in `platform/route-manifest.yaml`. No gates, no handoffs.
